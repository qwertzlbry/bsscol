## code to prepare `DATASET` dataset goes here
bss_colors <- c(
  `sky blue` = "#6bc5ff",
  `yellow`   = "#eeec22",
  `red`      = "#e5233d",
  `green`    = "#abe441",
  `blue`     = "#155f90")
usethis::use_data(bss_colors, overwrite = TRUE)
